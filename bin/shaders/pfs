#version 300 es

precision mediump float;

in vec2 Texcoord;
out vec3 outColor;
uniform sampler2D tex;
void main() {
    //outColor = vec3(Texcoord.x, Texcoord.y, 0);
    outColor = texture2D(tex, Texcoord).rgb;
}
